# Feature: <one-line description of the feature>

<one-paragraph problem statement: the user-facing change, why it matters, and any context the implementer needs to make judgment calls>

## User-facing change

<what the operator/end-user can do after this lands that they couldn't before — be concrete>

## Target file(s)

`<path/to/file.ts>` (or `<dir>/`)

## What to add

- <concrete instruction with file:line references where applicable>
- ...

## Constraints

1. <invariant that must hold — e.g., "no change to existing public APIs">
2. ...

## Acceptance criteria

- <observable, checkable outcome>
- New public surface is covered by minimal tests.

## Out of scope

- <what NOT to touch — adjacent features mentioned as "future work" go in their own runs>
- ...
