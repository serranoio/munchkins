# Synthetic feature fixture

Add a `multiply(a, b)` function to `src/math.ts`. Currently `src/math.ts` exports only `add` and has a `// TODO: multiply` placeholder where the new function should go.

This feature description is consumed by the feat-small agent's first step (the implementer). Under the harness, the agent's response is mocked — no real claude invocation occurs.
