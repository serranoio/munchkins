# Synthetic bug fixture

The function `add` in `src/math.ts` returns `a - b` instead of `a + b`. Fix it.

This bug description is consumed by the bugfix agent's first step. Under the harness, the agent's response is mocked — no real claude invocation occurs.
